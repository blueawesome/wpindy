{
  "name": "wpindy",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "cf:preview": "npm run build && wrangler dev",
    "cf:deploy": "npm run build && wrangler deploy",
    "cf:versions:upload": "npm run build && wrangler versions upload",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "alpinejs": "^3.15.12",
    "astro": "^6.1.0",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "wrangler": "^4.93.1"
  }
}
