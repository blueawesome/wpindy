{
  "name": "wpindy",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "cf:preview": "npm run build && wrangler pages dev dist",
    "cf:deploy": "npm run build && wrangler pages deploy dist --project-name wpindy",
    "cf:deploy:preview": "npm run build && wrangler pages deploy dist --project-name wpindy --branch preview",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "astro": "^6.1.0",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "wrangler": "^4.93.1"
  }
}
