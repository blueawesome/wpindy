---
import CaseStudyLayout from '../../layouts/CaseStudyLayout.astro';
import { work } from '../../data/work';

const item = work.find((w: any) => w.slug === 'finish-line-gutenberg-build');
---

<CaseStudyLayout item={item} />
